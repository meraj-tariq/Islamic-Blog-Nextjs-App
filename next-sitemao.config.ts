import type { IConfig } from 'next-sitemap';

const config: IConfig = {
  siteUrl: "https://www.yoursite.com", // Base URL of your website
  generateRobotsTxt: true, // Automatically generate a robots.txt file
};

export default config;